version: "3"
services:
  minio:
    image: minio/minio:latest
    ports:
      - "9000:9000" # api
      - "9001:9001" # web ui
    command: ["server", "/minio-data", "--console-address", ":9001"]
    tmpfs:
      - /data

  redis:
    image: redis
    ports:
      - "6379:6379"
    tmpfs:
      - /data
